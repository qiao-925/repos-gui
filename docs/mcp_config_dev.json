{
  "_comment": "CloneX MCP server config for source-mode (MVP). Copy into your agent client's MCP config file (Claude Desktop, Cursor, Windsurf). Replace `<path-to-CloneX-repo>` with the absolute path to your cloned CloneX repository.",
  "mcpServers": {
    "clonex": {
      "command": "uv",
      "args": ["run", "--extra", "mcp", "python", "-m", "clonex.mcp"],
      "cwd": "<path-to-CloneX-repo>"
    }
  }
}
